CREATE TYPE GUID_ITEMS AS TABLE ([Item] UNIQUEIDENTIFIER NOT NULL PRIMARY KEY)
CREATE TYPE INT_ITEMS AS TABLE ([Item] Int NOT NULL PRIMARY KEY)
CREATE TYPE TEXT_ITEMS AS TABLE ([Item] NVARCHAR(500) NOT NULL PRIMARY KEY)
CREATE TYPE DATE_ITEMS AS TABLE ([Item] DATE NOT NULL PRIMARY KEY)
CREATE TYPE BIT_ITEMS AS TABLE ([Item] bit NOT NULL PRIMARY KEY)